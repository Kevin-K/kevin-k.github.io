import React from 'react';
import Typography from '@material-ui/core/Typography';

export default function NotFoundPage() {
  return (<Typography variant="body1">404: Page not found.</Typography>);
}
